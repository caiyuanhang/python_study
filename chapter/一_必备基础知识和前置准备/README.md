# 一 必备基础知识和前置准备

[返回主目录](../../README.md)

## 1 必备基础知识

高级语言主要分为：**编译型**和**解释型**。

### 1.1 编译型

以 C 语言为例，过程如下图：

![编译型语言](./sources/编译型高级语言.png)

1. `Hello.c` 里面是我们编写的 c 语言代码，因为 CPU 不能执行高级语言代码，所以有了下面步骤
2. 编译器负责编译 `Hello.c`，生成汇编代码（CPU 依旧不能运行）
3. 汇编代码会被汇编器汇编，生成二进制机器码。注意：此时的机器码还只是零散的代码片段，缺少很多完整的结构和一些外部函数地址，而这些内容由链接器完成
4. 链接器会将这些零散的二进制代码片段（机器码）和外部函数地址链接起来，拼成完整的结构
5. 最终生成可直接运行的可执行文件，比如：Windows 系统的 `.exe` 文件
6. 对于多台要用到这个可执行文件的电脑，只需分别复制过去即可，而不需要再进行以上步骤。注意：这里的多台电脑要求操作系统和 CPU 架构均相同

**优点：**

同一运行平台，代码只需编译一次，执行效率高

**缺点：**

跨平台性差，大型项目编译时间较长，开发效率略低

### 1.2 解释型

以 Python 语言为例，过程如下图：

![编译型语言](./sources/解释型高级语言.png)

Hello.py 同样不能直接被 CPU 执行，需要被翻译成被 CPU 识别的机器码，只不过翻译的过程没有编译型语言那么麻烦，只需安装一个 python 解释器即可。

python 解释器可以对 python 代码逐行读取和翻译，将 python 代码直接翻译成 CPU 可以执行的操作代码。

如果多台电脑要运行 python 代码，只需要在各个电脑上安装 python 解释器即可，在不同的平台可以安装不同版本的 python 解释器，比如：Windows X86 架构系统需要安装 X86 版本的 python 解释器，ARM 架构系统则需要安装 ARM 版本的 python 解释器，同理 Linux 和 Mac 系统也是如此。

**优点：**

跨平台性好，无需编译，开发调试灵活高效

**缺点：**

每次运行都需要重新解释，执行效率较低

## 2 前置准备 - 搭建 Python 开发环境

### 2.1 下载 Python 解释器

在 [python 的官方下载地址](https://www.python.org/downloads/) 可以下载最新版的 python 解释器，用来运行 python 代码。下载如图：

![python 解释器的下载](./sources/python解释器的下载.png)

可嵌入式安装包，就是跟你的项目打包到一起的安装包，在安装项目的时候，会连带着这个 python 解释器的安装包一起安装。

安装完成之后，通过 `python --version` 即可查到对应的版本信息

以上是 Windows 系统的安装，Mac 系统的安装也大差不差，主要是在 python 命令的不同：

Windows 系统的 python 命令是 `python --version`，Mac 的是 `python3 --version`。

如果想 Mac 和 Windows 一样都是使用 `python --version`，可以通过下面代码设置：

`sudo echo 'alias python="python3"' >> .zshrc`

### 2.2 Python 解释器的使用

在终端通过 `python` 命令可以进入 python 交互式解释器（python shell），如下图：

![python 交互式解释器](./sources/python交互式解释器.png)

在这里可以输入一段 python 代码（在 `>>>` 的右侧输入），并快速得到代码的执行结果。

如果想退出该界面，可以输入 `exit()` 或 `Ctrl + Z` 后，按下回车键即可退出。

除了交互式界面，也可以直接运行一个 python 文件，比如：现有一个 `test.py` 文件，在该文件目录下打开终端，通过 `python test.py` 即可运行该文件。

### 2.3 安装 PyCharm

PyCharm 是一套用于 python 的集成开发环境，主要提供以下功能：

- 可自定义的代码编辑器
- 自动补全 + 语法高亮
- 提供调试器，便于快速找出代码错误
- 组织文件、管理工程结构

通过 [PyCharm 的官方下载地址](https://www.jetbrains.com/zh-cn/pycharm/download/?section=windows)，可以下载一个完整版，其中包含 1 个月的 pro 版本，pro 版本到期后，可以继续使用免费版

如学业需要，可用魔法工具。详情见 PyCharm_magic 文件夹，使用魔术之前，需要先**断网**以及将地区设置为**未指定**

### 2.4 设置 PyCharm

#### PyCharm 界面说明

![PyCharm 界面说明](./sources/PyCharm界面说明.png)

#### 设置语言和区域

![打开设置](./sources/设置语言和区域_01.png)

![设置语言和区域](./sources/设置语言和区域_02.png)

#### 设置 PyCharm 的主题

![设置 PyCharm 的主题](./sources/设置PyCharm的主题.png)

#### 设置代码编辑区字体大小

通过快捷键 `Alt + Shift + .` 调大和 `Alt + Shift + ,` 调小 可以快速设置代码编辑区的字体大小。

除此之外，还可以在设置中来设置字体大小，主要有两种方式：

- 正常设置字体大小，如图（当有“在配色方案中定义”的黄色提示时，此设置不生效）：
  ![正常设置字体大小](./sources/设置代码编辑区字体大小_01.png)

- 在配色方案中设置字体大小
  ![在配色方案中设置字体大小](./sources/设置代码编辑区字体大小_02.png)

- 正常和在配色方案中设置字体大小，两者都设置的情况下，配色方案字体大小设置的优先级更高，如果要取消配色方案的优先级，取消勾选“使用配色方案字体，而不是默认”选项（如下图），此时将应用正常设置的字体大小，而不是配色方案中设置的字体大小
  ![在配色方案中设置字体大小](./sources/设置代码编辑区字体大小_03.png)

### 2.5 新建项目和文件

#### 新建项目

![打开新建项目页面](./sources/新建项目_01.png)

![新建项目配置](./sources/新建项目_02.png)

#### 取消防火墙对项目的监控

如果电脑的防火墙等级比较高，对于新创建的项目，PyCharm 右下角会弹出如下图的窗口提示：

![PyCharm 防火墙不信任新创建项目文件夹提示](./sources/防火墙不信任新创建项目文件夹_01.png)

提示的意思是：由于电脑的防病毒系统并不信任当前文件夹，将会对此文件夹进行实时扫描，这会影响 PyCharm 的性能。

对于我们的项目文件夹，可以将它排除在防病毒系统的不信任名单之外，这样就不会影响 PyCharm 的性能。

点击 **排除文件夹**，对弹出的下图对话框点击 **是** 即可

![将项目文件夹排除在防病毒系统的不信任名单之外](./sources/防火墙不信任新创建项目文件夹_02.png)

#### 新建项目的初始文件夹

新建项目的会有两个初始文件夹：`.idea` 和 `.venv`

![新建项目的初始文件夹](./sources/新建项目的初始文件夹.png)

`.idea` 文件夹存放当前项目在 PyCharm 中的各种配置信息文件，比如：

- 记录关闭 PyCharm 的时候，输入光标的位置。关闭时在第 8 行，再次打开项目就还是在第 8 行
- 记录面板布局信息
- 记录项目目录的展开层级
- 等等各类信息...

`.venv` 文件夹存放当前项目的虚拟环境，这里的虚拟环境就是每个项目自己独有的一套运行环境，这样可以让项目之间相互独立、没有干扰。

备注：PyCharm 在呈现项目结构的时候会自动把 `.idea` 文件夹隐藏起来，但 `.venv` 文件夹不会隐藏

#### 新建 python 文件

![新建 python 文件](./sources/新建python文件_01.png)

![输入新建 python 文件名](./sources/新建python文件_02.png)

#### 运行 python 文件代码

![运行 python 文件代码](./sources/运行python文件代码_01.png)

![在终端查看代码运行结果](./sources/运行python文件代码_02.png)

### 2.6 PyCharm 的快捷键

#### 常用快捷键

| 快捷键 | 对应操作 |
| --- | --- |
| `Ctrl + /` | 行注释 |
| `Ctrl + C` | 复制当前行 / 复制选定代码 |
| `Ctrl + X` | 剪切当前行 / 剪切选定代码 |
| `Ctrl + D` | 重复当前行 / 重复选定代码 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Y` | 删除当前行 / 反撤销 |
| `Crtl + P` | 查看方法传参 |
| `Ctrl + F / R` | 查找 / 替换 |
| `Shift + Alt + , / .` | 减少 / 增大代码编辑区字体 |
| `Ctrl + Backspace / Delete` | 向前 / 向后删除一个单词 |
| `Tab / Shift + Tab` | 缩进 / 反向缩进 |
| `Shift + Enter` | 换行（即使光标不在行尾） |
| `Ctrl + Alt + Enter` | 在当前行上行添加一行（光标在当前行任意位置都可以） |
| `Ctrl + = / -` | 展开 / 折叠当前代码块 |
| `Ctrl + Shift + ↑ / ↓` | 将当前行上移 / 下移 |
| `Ctrl + Shift + L` | 格式化代码 |

#### 如何设置快捷键

##### 通过搜索功能设置快捷键

![设置快捷键](./sources/快捷键设置_01.png)

##### 通过搜索快捷键设置功能

![设置快捷键](./sources/快捷键设置_02.png)

#### 恢复默认快捷键

![设置快捷键](./sources/快捷键设置_03.png)

#### 单独设置控制台字体

![设置快捷键](./sources/快捷键设置_04.png)

#### 导入和到处 PyCharm 设置

导出的设置是一个 `zip` 包，导入时也需要导入一个设置的 `zip` 包

![设置快捷键](./sources/导入和导出设置.png)
